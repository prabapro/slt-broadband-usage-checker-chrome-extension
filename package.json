{
	"name": "slt-bb-usage-checker",
	"private": true,
	"version": "1.2.8",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "node build.js",
		"build:prod": "npm run clean:dist_zip && cross-env NODE_ENV=production node build.js && npm run unzip:prod",
		"preview": "vite preview",
		"clean:dist_zip": "node scripts/cleanFolder.js dist_zip",
		"clean": "node scripts/cleanFolder.js dist",
		"unzip:prod": "node scripts/unzipProd.js",
		"test": "jest",
		"test:watch": "jest --watch",
		"test:coverage": "jest --coverage"
	},
	"devDependencies": {
		"@babel/core": "^7.25.2",
		"@babel/preset-env": "^7.25.4",
		"@testing-library/jest-dom": "^6.5.0",
		"adm-zip": "^0.5.16",
		"archiver": "^7.0.1",
		"babel-jest": "^29.5.0",
		"cross-env": "^7.0.3",
		"dotenv": "^16.4.5",
		"fs-extra": "^10.0.0",
		"html-minifier-terser": "^7.2.0",
		"jest": "^29.5.0",
		"jest-environment-jsdom": "^29.5.0",
		"vite": "^5.4.3"
	}
}
